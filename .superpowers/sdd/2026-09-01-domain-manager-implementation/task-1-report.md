# Task 1 report: scaffold the Tauri/React project and verification harness

Status: `DONE_WITH_CONCERNS`

## Commit

- `1bf6371` — `chore: scaffold domain manager desktop app`

## Files changed

Created:

- `package.json`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml` (generated by `pnpm approve-builds` so the installed vgpu native packages may run their postinstall scripts)
- `index.html`
- `vite.config.ts`
- `vitest.config.ts`
- `tsconfig.json`
- `tsconfig.app.json`
- `tsconfig.node.json`
- `src/main.tsx`
- `src/vite-env.d.ts`
- `src/app/App.tsx`
- `src/app/AppProviders.tsx`
- `src/app/App.test.tsx`
- `src/test/setup.ts`
- `src-tauri/Cargo.toml`
- `src-tauri/build.rs`
- `src-tauri/tauri.conf.json`
- `src-tauri/src/main.rs`
- `src-tauri/src/lib.rs`
- `src-tauri/capabilities/default.json`

Modified:

- `.gitignore` (all existing ignore rules preserved; Task 1 dependency/build/data rules added)

No `Cargo.lock` was generated because this host has no Rust toolchain.

## TDD evidence

1. Wrote `src/app/App.test.tsx` before the `App` implementation.
2. Red run: `CI=true pnpm vitest run src/app/App.test.tsx`

   Observed failure:

   ```text
   FAIL  src/app/App.test.tsx [ src/app/App.test.tsx ]
   Error: Cannot find module './App' imported from .../src/app/App.test.tsx
   ```

   This was the intended missing-shell failure.
3. Added the minimal `App` heading, providers passthrough, and StrictMode root mount.
4. Green run: `CI=true pnpm vitest run src/app/App.test.tsx`

   ```text
   Test Files  1 passed (1)
   Tests  1 passed (1)
   ```

## Dependency installation

- `pnpm --version` resolved and installed Corepack's pnpm `11.25.0`.
- The brief's runtime dependency command installed the listed runtime dependencies, but pnpm initially exited with `ERR_PNPM_IGNORED_BUILDS` for `@vgpu/adapter-node` and `webgpu`; both were approved with `pnpm approve-builds`.
- The brief's exact development dependency command could not resolve `@vgpu/cli`:

  ```text
  ERR_PNPM_FETCH_404 GET https://registry.npmjs.org/@vgpu%2Fcli: Not Found - 404
  @vgpu/cli is not in the npm registry, or you have no permission to fetch it.
  ```

  The other development dependencies, including `@vgpu/wgsl`, were installed. The unavailable private package was not fabricated or added to the lockfile. The public `vgpu` package remains installed and exposes its `vgpu` CLI.

## Verification commands and observed results

Because this non-interactive host's pnpm dependency-status check prompts to rebuild `node_modules`, the passing frontend commands were run with `CI=true`; this is the same command/script entry point with the non-interactive install policy enabled.

- `CI=true pnpm vitest run src/app/App.test.tsx` — PASS, 1 test.
- `CI=true pnpm test` — PASS, 1 test.

  ```text
  Test Files  1 passed (1)
  Tests  1 passed (1)
  ```

- `CI=true pnpm typecheck` — PASS (`tsc -b --pretty false`).
- `CI=true pnpm build` — PASS; Vite created `dist/index.html` and the production JS bundle.
- `CI=true pnpm tauri --version` — PASS (`tauri-cli 2.11.4`).
- Manifest/config validation via Node JSON parsing and script/packageManager assertions — PASS.
- `git diff --cached --check` — PASS before commit.
- `cargo test --manifest-path src-tauri/Cargo.toml` — BLOCKED by host: `zsh:1: command not found: cargo` (exit 127).
- `cargo check --manifest-path src-tauri/Cargo.toml` — BLOCKED by host: `zsh:1: command not found: cargo` (exit 127).

## Acceptance review

- Package scripts match the brief exactly, and `packageManager` is `pnpm@11.25.0`.
- Tauri config uses package `domain-manager`, identifier `com.dongqing.domainmanager`, product `DOMAIN.`, dev URL `http://localhost:5173`, `pnpm dev`, and `pnpm build`.
- Capabilities contain only `core:default`, `dialog:default`, `opener:default`, and `log:default`; no embedded browser, cloud, telemetry, or encryption behavior was added.
- App shell renders the required `网站收藏库` heading and is mounted through `StrictMode` and the currently transparent `AppProviders`.
- No later feature task was implemented.
- Commit uses the exact required message.

## Self-review and concerns

- Existing `.gitignore` rules were restored and preserved after review; only foundation-related additions remain.
- Rust source/manifests were kept minimal, but cannot be compiled on this host. After installing Rust stable and the Tauri prerequisites, run:

  ```bash
  cargo generate-lockfile --manifest-path src-tauri/Cargo.toml
  cargo test --manifest-path src-tauri/Cargo.toml
  cargo check --manifest-path src-tauri/Cargo.toml
  ```

  Then commit the generated `src-tauri/Cargo.lock` if required by the global implementation plan.
- `@vgpu/cli` remains unresolved because it is not published to the configured npm registries; Task 2 should decide whether to rely on the installed public `vgpu` CLI or provide an approved repository source.
- In this host's non-TTY environment, unprefixed pnpm run commands may prompt for a dependency-directory rebuild because pnpm reports the `enableGlobalVirtualStore` setting changed. The CI-prefixed commands above pass without that interactive prompt.

## Fix round 1

- `src-tauri/Cargo.lock` has now been generated and is included in the Task 1 correction commit.
- `cargo test --manifest-path src-tauri/Cargo.toml` and `cargo check --manifest-path src-tauri/Cargo.toml` were attempted in the earlier verification round but were blocked because `cargo` was unavailable (exit 127). They were not rerun in this short correction round; no Rust PASS is claimed.
- The implementation plan now records the registry finding for unavailable `@vgpu/cli` and the decision to use the installed public `vgpu` CLI. `package.json` does not include `@vgpu/cli`.
- Tauri bundle targets are corrected to `["app", "dmg", "nsis"]`.
- The previously observed frontend PASS results for the focused test, full test, typecheck, build, and Tauri CLI version are retained; those commands were not rerun in this correction round.
